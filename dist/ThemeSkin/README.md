# ThemeSkin dist package (placeholder)

This folder holds the deployable skin. Copy everything from `src/skin/` here, then run `npm run build` to place `css/skin.min.css` in this folder before zipping/deploying to DNN.
